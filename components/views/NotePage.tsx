import {Button, Text, View} from "react-native";
import React from "react";

function NotesPage({ navigation }) {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>Notes</Text>

        </View>
    );
}

export default NotesPage;